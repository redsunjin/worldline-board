{
  "name": "worldline-board",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node scripts/check-public-boundary.mjs",
    "ci": "npm run check && npm test"
  }
}
